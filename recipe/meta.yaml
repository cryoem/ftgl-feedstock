{% set commit_hash = '1556c3f13695eeb90425967f3b6941348e0ca087' %}

package:
  name: "ftgl"
  version: 2.1.3

source:
  url: https://github.com/cryoem/ftgl/archive/{{ commit_hash }}.zip
  fn: ftgl-{{ commit_hash }}.zip

build:
  number: 2

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ cdt('mesa-libgl-devel') }}   # [linux]
    - cmake
    - gnuconfig
    - autoconf
    - automake
    - libtool

  host:
    - freetype
    - libglu  # [linux]
  
  run:
    - freetype

about:
  home: http://sourceforge.net/projects/ftgl/
  license: MIT
