{% set commit_hash = '6ce3b10bdc907bd27d97d8292e75be9da2dec914' %}

package:
  name: "ftgl"
  version: 2.1.3

source:
  url: https://github.com/cryoem/ftgl/archive/{{ commit_hash }}.zip
  fn: ftgl-{{ commit_hash }}.zip

build:
  number: 2

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ cdt('mesa-libgl-devel') }}   # [linux]
    - cmake

  host:
    - freetype
    - libglu  # [linux]
  
  run:
    - freetype

about:
  home: http://sourceforge.net/projects/ftgl/
  license: MIT
