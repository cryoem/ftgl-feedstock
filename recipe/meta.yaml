{% set commit_hash = '42a3099cf8ff90e03b258d85efce4d30fee7b31d' %}

package:
  name: "ftgl"
  version: 2.1.3

source:
  url: https://github.com/cryoem/ftgl/archive/{{ commit_hash }}.zip
  fn: ftgl-{{ commit_hash }}.zip

build:
  number: 2

requirements:
  build:
    - {{ compiler('cxx') }}
    - {{ cdt('mesa-libgl-devel') }}   # [linux]
    - cmake

  host:
    - freetype
    - libglu  # [linux]
  
  run:
    - freetype

about:
  home: http://sourceforge.net/projects/ftgl/
  license: MIT
